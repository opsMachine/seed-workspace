# ─────────────────────────────────────────────────────────────────────
# transcript-vault-mcp — environment
# Copy to `.env` and fill in. NEVER commit `.env`.
# ─────────────────────────────────────────────────────────────────────

# Which recorder adapter to use. See adapters/README.md.
# Default: fathom. Other shipped stubs: otter, granola, fireflies.
RECORDER_ADAPTER=fathom

# Where the corpus lives. Defaults to this repo root if unset, so a
# fresh clone keeps working with no env setup.
#
# Recommended: point at the parent workspace's vault folder so the
# corpus and your strategy artifacts live in one place:
#
#   VAULT_DATA_ROOT=/home/you/Documents/GitHub/seed-workspace/vault
#
# (Legacy alias: FATHOM_DATA_ROOT — still honored for back-compat.)
#
# IMPORTANT — gitignore scope: the seed-workspace .gitignore only protects
# paths INSIDE the workspace directory. If you set VAULT_DATA_ROOT to a path
# outside the workspace, the gitignore rules there do NOT apply and your
# raw transcripts, vectors, and sync state will not be automatically excluded
# from any git repo that might contain that path. Either keep VAULT_DATA_ROOT
# inside seed-workspace/vault/ (recommended), or ensure that external path is
# in a directory that is itself untracked.
VAULT_DATA_ROOT=

# ─────────────────────────────────────────────────────────────────────
# Adapter-specific keys (set what you need)
# ─────────────────────────────────────────────────────────────────────
#
# Choose one of two modes for recorder API keys:
#
# MODE A — Direct (simple): put the real key here. Only you run npm run extract.
# MODE B — OneCLI gateway (recommended): key stays encrypted in vault; set placeholder
#   here and configure ONECLI_* below. See scripts/onecli-proxy-env.sh.
#   https://github.com/onecli/onecli
#
# Never commit real values. See seed-workspace PHILOSOPHY.md (Credential security).

# Fathom — MODE A: real key | MODE B: placeholder "Fathom"
# Issue from https://app.fathom.video/settings/integrations
FATHOM_API_KEY=

# ─────────────────────────────────────────────────────────────────────
# OneCLI gateway (MODE B only) — leave blank to use direct mode
# ─────────────────────────────────────────────────────────────────────
# Gateway URL (default: http://127.0.0.1:10255)
ONECLI_GATEWAY=http://127.0.0.1:10255
# Agent access token — from OneCLI dashboard Agents page (aoc_ prefix)
ONECLI_AOC_TOKEN=
# Management token — from OneCLI dashboard (oc_ prefix); used for vault:check
ONECLI_AGENT_ACCESS_TOKEN=
# CA cert path — extracted once: docker cp onecli:/app/data/gateway/ca.pem ~/.onecli/gateway-ca.pem
# ONECLI_CA=/home/you/.onecli/gateway-ca.pem

# Fireflies — issue from https://app.fireflies.ai/settings/developer
FIREFLIES_API_KEY=

# Otter — set if you have a working API key OR use the export-folder path
OTTER_API_KEY=
OTTER_EXPORT_DIR=

# Granola — typically reads from the local on-disk store
GRANOLA_LOCAL_STORE=
